import pygame
from pygame.font import init, SysFont, get_default_font

# Start fonts. It's necessary
init()
TITLE = SysFont("Arial", 64)
BIG_MENU = SysFont("Arial", 34)

HEALTHBAR = SysFont("Helvetica", 28)
DEFAULT = SysFont("Arial", 24)
